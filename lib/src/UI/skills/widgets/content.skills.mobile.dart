import 'package:flutter/material.dart';

class ContentSkillsMobile extends StatelessWidget {
  const ContentSkillsMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return Column();
  }
}